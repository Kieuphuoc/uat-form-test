# Same-origin via Vite proxy → Form.Api :5600 (cookie keycloak gửi kèm khi domain=localhost)
VITE_API_URL=/api
# Khớp Form:Files:BaseUrl — resolve /api/files/{id} khi mở/prev
VITE_FILES_BASE_URL=http://localhost:5200
